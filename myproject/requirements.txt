Django>=4.2,<5.0
gunicorn              # WSGI server for running Django
whitenoise            # Serve static files in production
psycopg2-binary       # If using PostgreSQL (Render offers PostgreSQL)

# Optional but recommended
python-decouple       # Manage secrets from environment variables
